This is the API built with Ruby on Rails.

## How to Install

In the project directory, you run:

### `bundle install` 

## How to run

In the project directory, you can run:

### `rails server -p 3001`
